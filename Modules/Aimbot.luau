local a = {} function a.new(b) local Players = b.Players local c = b.RunService local d = b.Workspace local e = b.Player local f = b.UserInputService local g = b.Utils local h = b.Settings local i = e:GetMouse() local j = d.CurrentCamera local k = nil local l = false local m = 0 local n = nil local o = nil local p = false local q = false local r = false local s = false local t = nil local u = nil local v = RaycastParams.new() local w = {} local function x(y) local z = (((type(getgenv) == "function") and getgenv()) or _G) return ((z and z[y]) or _G[y]) end local aa = Drawing.new("Circle") aa.Thickness = 2 aa.Filled = false aa.Visible = false local function ab(ac) local ad = (h.ExcludeTeams or {}) local ae = g.GetTeamName(ac) if ad[ae] then return true end return (((h.TeamCheck and ac.Team) and e.Team) and (ac.Team == e.Team)) end local function af(ag) local ah = (h.PrioritizeTeams or {}) return ((ah[g.GetTeamName(ag)] and 1) or 0) end local function ai(aj, ak) local al = g.GetHumanoid(aj) if (not al or (al.Health <= 0)) then return false end return ((ak or not h.HealthCheck) or (al.Health >= h.MinHealth)) end local function am(an) if not h.FriendCheck then return false end return g.IsFriend(e, an) end local ao ao = function(ap, aq, ar) local as = g.GetCharacterPart(ap, "Head") if not as then return true end local at = j.CFrame.Position local au = (as.Position - at) local av = (ar or RaycastParams.new()) local aw = {e.Character} if (aq and not ar) then for ax, ay in ipairs(Players:GetPlayers()) do if ((ay ~= e) and ay.Character) then table.insert(aw, ay.Character) end end end if not ar then av.FilterDescendantsInstances = aw end av.FilterType = Enum.RaycastFilterType.Blacklist local az = d:Raycast(at, au, av) return ((az ~= nil) and not az.Instance:IsDescendantOf(ap)) end local function ba(bb, bc, bd, be, bf) if (not bb or (bb == e)) then return false end local bg = bb.Character local bh = g.GetHead(bb) if (not bg or not bh) then return false end local bi = math.clamp((tonumber(h.Range) or 500), 1, 1500) if (((bh.Position - j.CFrame.Position)).Magnitude > bi) then return false end if ab(bb) then return false end if am(bb) then return false end if not ai(bb, bd) then return false end local bj, bk = j:WorldToViewportPoint(bh.Position) if (not bd and (((bj.Z <= 0) or not bk))) then return false end if (not bd and bc) then local bl = f:GetMouseLocation() local bm = Vector2.new(bj.X, bj.Y) if (((bm - bl)).Magnitude > h.Fov) then return false end end if ((not bd and h.WallCheck) and ao(bg, be, bf)) then return false end return true end local function bn() local bo = {} local bp = f:GetMouseLocation() local bq = j.CFrame.Position for br, bs in ipairs(Players:GetPlayers()) do if ba(bs, true) then local bt = bs.Character.Head local bu = j:WorldToViewportPoint(bt.Position) local bv = Vector2.new(bu.X, bu.Y) local bw = ((bv - bp)).Magnitude local bx = ((bt.Position - bq)).Magnitude table.insert(bo, {target = bs, priority = af(bs), cursorDistance = bw, playerDistance = bx}) end end if (#bo == 0) then return nil end local by = (h.Targeting or "Normal") if (by == "Random") then return bo[math.random(1, #bo)].target end local bz = bo[1] for ca = 2, #bo do local cb = bo[ca] local cc = false if (by == "Closest") then cc = (cb.playerDistance < bz.playerDistance) elseif (by == "Furthest") then cc = (cb.playerDistance > bz.playerDistance) else cc = ((cb.priority > bz.priority) or (((cb.priority == bz.priority) and (((cb.cursorDistance < bz.cursorDistance) or (((cb.cursorDistance == bz.cursorDistance) and (cb.playerDistance < bz.playerDistance)))))))) end if cc then bz = cb end end return bz.target end local function cd() if not h.LockAim then k = bn() return k end if (k and ba(k, false, true)) then return k end k = bn() return k end local function ce(cf) local cg = (cf and cf.Character) local ch = g.GetHead(cf) local ci = g.GetRootPart(cf) if not ch then return nil end if (not ci or (h.Prediction <= 0)) then return ch.Position end return (ch.Position + ((ci.AssemblyLinearVelocity * h.Prediction))) end local function cj(ck) local cl = h.Bind if (typeof(cl) == "EnumItem") then return ((ck.KeyCode == cl) or (ck.UserInputType == cl)) end if (type(cl) ~= "string") then return false end cl = string.upper(cl) cl = string.gsub(cl, "^ENUM%.KEYCODE%.", "") cl = string.gsub(cl, "^ENUM%.USERINPUTTYPE%.", "") if ((cl == "MB1") or (cl == "MOUSEBUTTON1")) then return (ck.UserInputType == Enum.UserInputType.MouseButton1) end if ((cl == "MB2") or (cl == "MOUSEBUTTON2")) then return (ck.UserInputType == Enum.UserInputType.MouseButton2) end return ((ck.KeyCode.Name == cl) or (ck.UserInputType.Name == cl)) end local function cm() local cn = h.Bind if (typeof(cn) == "EnumItem") then if (cn.EnumType == Enum.KeyCode) then return f:IsKeyDown(cn) end return f:IsMouseButtonPressed(cn) end if (type(cn) ~= "string") then return false end cn = string.upper(cn) cn = string.gsub(cn, "^ENUM%.KEYCODE%.", "") cn = string.gsub(cn, "^ENUM%.USERINPUTTYPE%.", "") if ((cn == "MB1") or (cn == "MOUSEBUTTON1")) then return f:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) end if ((cn == "MB2") or (cn == "MOUSEBUTTON2")) then return f:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) end local co = Enum.KeyCode[cn] if co then return f:IsKeyDown(co) end return false end local function cp() local cq = e.Character local cr = (cq and cq:FindFirstChild("Head")) if (e.CameraMode == Enum.CameraMode.LockFirstPerson) then return true end return ((cr and (((j.CFrame.Position - cr.Position)).Magnitude <= 1.5)) or false) end local function cs() if p then if q then r = cp() f.MouseBehavior = ((r and o) or Enum.MouseBehavior.Default) else f.MouseBehavior = o end o = nil p = false q = false end end local function ct() l = false k = nil t = nil u = nil s = false cs() n = nil end local function cu() return math.clamp((tonumber(h.TriggerbotDelay) or 0), 0, 0.3) end local function cv() t = nil u = nil end local function cw() local cx = x("mouse1press") local cy = x("mouse1release") if ((type(cx) ~= "function") or (type(cy) ~= "function")) then return false end local cz = pcall(cx) local da = pcall(cy) return (cz and da) end local function db(dc) local dd, de = j:WorldToViewportPoint(dc.Position) if (not de or (dd.Z <= 0)) then return nil end local df = j:WorldToViewportPoint((dc.Position + Vector3.new((dc.Size.X / 2), 0, 0))) local dg = ((Vector2.new(df.X, df.Y) - Vector2.new(dd.X, dd.Y))).Magnitude return math.max((dg * 1.25), 6) end local function dh() local di = f:GetMouseLocation() local dj = nil local dk = math.huge local dl = v local dm = w table.clear(dm) table.insert(dm, e.Character) for dn, dp in ipairs(Players:GetPlayers()) do if ((dp ~= e) and dp.Character) then table.insert(dm, dp.Character) end end dl.FilterDescendantsInstances = dm dl.FilterType = Enum.RaycastFilterType.Blacklist for dq, dr in ipairs(Players:GetPlayers()) do local ds = g.GetHead(dr) if (ds and ba(dr, false, false, true, dl)) then local dt, du = j:WorldToViewportPoint(ds.Position) local dv = ((Vector2.new(dt.X, dt.Y) - di)).Magnitude local dw = db(ds) if (((du and dw) and (dv <= dw)) and (dv < dk)) then dj = dr dk = dv end end end return dj end local function dx() if not h.Triggerbot then cv() return end local dy = dh() if not dy then cv() return end local dz = os.clock() if (t ~= dy) then t = dy u = (dz + cu()) end if (u and (dz >= u)) then if not cw() then cv() return end u = dz end end local function ea() if (p or s) then return end o = (n or f.MouseBehavior) if (cp() and (o ~= Enum.MouseBehavior.LockCenter)) then o = Enum.MouseBehavior.LockCenter end q = cp() p = true r = false f.MouseBehavior = Enum.MouseBehavior.LockCenter end local function eb() local ec = h local ed = f:GetMouseLocation() aa.Position = ed aa.Radius = ec.Fov aa.Visible = ec.Enabled if ec.RainbowFov then m = (((m + ec.RainbowSpeed)) % 1) aa.Color = Color3.fromHSV(m, 1, 1) elseif (k and l) then aa.Color = ec.TargetedFovColor else aa.Color = ec.FovColor end end local eg = f.InputBegan:Connect(function(ee, ef) if (not h.Enabled or not cj(ee)) then return end if (not cp() and (f.MouseBehavior ~= Enum.MouseBehavior.LockCenter)) then return end l = true n = f.MouseBehavior if (cp() and (n ~= Enum.MouseBehavior.LockCenter)) then n = Enum.MouseBehavior.LockCenter end s = false end) local ei = f.InputEnded:Connect(function(eh) if not cj(eh) then return end ct() end) local ej = {} function ej:Update(ek) j = d.CurrentCamera if not j then return end eb() if (not h.Enabled or not l) then ct() if (r and not cp()) then f.MouseBehavior = Enum.MouseBehavior.Default r = false end else if not cm() then ct() return end if ((q and not cp()) and p) then cs() s = true r = false return end local el = cd() local em = ce(el) if em then ea() local en = (math.clamp((tonumber(h.Smoothing) or 5), 1, 100) / 100) local eo = CFrame.new(j.CFrame.Position, em) j.CFrame = j.CFrame:Lerp(eo, en) else k = nil cs() end end dx() end function ej:Destroy() eg:Disconnect() ei:Disconnect() aa.Visible = false aa:Remove() ct() end return ej end return a