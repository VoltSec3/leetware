local a = {} function a.new(b) local Players = b.Players local c = b.RunService local d = b.Workspace local e = b.Player local f = b.UserInputService local g = b.Utils local h = b.Settings local i = e:GetMouse() local j = d.CurrentCamera local k = nil local l = false local m = 0 local n = nil local o = nil local p = false local q = false local r = false local s = false local t = nil local u = nil local function v(w) local x = (((type(getgenv) == "function") and getgenv()) or _G) return ((x and x[w]) or _G[w]) end local y = Drawing.new("Circle") y.Thickness = 2 y.Filled = false y.Visible = false local function z(aa) local ab = (h.ExcludeTeams or {}) local ac = g.GetTeamName(aa) if ab[ac] then return true end return (((h.TeamCheck and aa.Team) and e.Team) and (aa.Team == e.Team)) end local function ad(ae) local af = (h.PrioritizeTeams or {}) return ((af[g.GetTeamName(ae)] and 1) or 0) end local function ag(ah, ai) local aj = g.GetHumanoid(ah) if (not aj or (aj.Health <= 0)) then return false end return ((ai or not h.HealthCheck) or (aj.Health >= h.MinHealth)) end local function ak(al) if not h.FriendCheck then return false end return g.IsFriend(e, al) end local am am = function(an) local ao = g.GetCharacterPart(an, "Head") if not ao then return true end local ap = j.CFrame.Position local aq = (ao.Position - ap) local ar = RaycastParams.new() ar.FilterDescendantsInstances = {e.Character} ar.FilterType = Enum.RaycastFilterType.Blacklist local as = d:Raycast(ap, aq, ar) return ((as ~= nil) and not as.Instance:IsDescendantOf(an)) end local function at(au, av, aw) if (not au or (au == e)) then return false end local ax = au.Character local ay = g.GetHead(au) if (not ax or not ay) then return false end if z(au) then return false end if ak(au) then return false end if not ag(au, aw) then return false end local az, ba = j:WorldToViewportPoint(ay.Position) if (not aw and (((az.Z <= 0) or not ba))) then return false end if (not aw and av) then local bb = f:GetMouseLocation() local bc = Vector2.new(az.X, az.Y) if (((bc - bb)).Magnitude > h.Fov) then return false end end if ((not aw and h.WallCheck) and am(ax)) then return false end return true end local function bd() local be = nil local bf = -1 local bg = h.Fov local bh = math.huge local bi = f:GetMouseLocation() local bj = j.CFrame.Position for bk, bl in ipairs(Players:GetPlayers()) do if at(bl, true) then local bm = ad(bl) local bn = bl.Character.Head local bo = j:WorldToViewportPoint(bn.Position) local bp = Vector2.new(bo.X, bo.Y) local bq = ((bp - bi)).Magnitude local br = ((bn.Position - bj)).Magnitude if (bm > bf) then bf = bm bg = bq bh = br be = bl elseif ((bm == bf) and (((bq < bg) or (((bq == bg) and (br < bh)))))) then bg = bq bh = br be = bl end end end return be end local function bs() if not h.LockAim then k = bd() return k end if (k and at(k, false, true)) then return k end k = bd() return k end local function bt(bu) local bv = (bu and bu.Character) local bw = g.GetHead(bu) local bx = g.GetRootPart(bu) if not bw then return nil end if (not bx or (h.Prediction <= 0)) then return bw.Position end return (bw.Position + ((bx.AssemblyLinearVelocity * h.Prediction))) end local function by(bz) local ca = h.Bind if (typeof(ca) == "EnumItem") then return ((bz.KeyCode == ca) or (bz.UserInputType == ca)) end if (type(ca) ~= "string") then return false end ca = string.upper(ca) ca = string.gsub(ca, "^ENUM%.KEYCODE%.", "") ca = string.gsub(ca, "^ENUM%.USERINPUTTYPE%.", "") if ((ca == "MB1") or (ca == "MOUSEBUTTON1")) then return (bz.UserInputType == Enum.UserInputType.MouseButton1) end if ((ca == "MB2") or (ca == "MOUSEBUTTON2")) then return (bz.UserInputType == Enum.UserInputType.MouseButton2) end return ((bz.KeyCode.Name == ca) or (bz.UserInputType.Name == ca)) end local function cb() local cc = h.Bind if (typeof(cc) == "EnumItem") then if (cc.EnumType == Enum.KeyCode) then return f:IsKeyDown(cc) end return f:IsMouseButtonPressed(cc) end if (type(cc) ~= "string") then return false end cc = string.upper(cc) cc = string.gsub(cc, "^ENUM%.KEYCODE%.", "") cc = string.gsub(cc, "^ENUM%.USERINPUTTYPE%.", "") if ((cc == "MB1") or (cc == "MOUSEBUTTON1")) then return f:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) end if ((cc == "MB2") or (cc == "MOUSEBUTTON2")) then return f:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) end local cd = Enum.KeyCode[cc] if cd then return f:IsKeyDown(cd) end return false end local function ce() local cf = e.Character local cg = (cf and cf:FindFirstChild("Head")) if (e.CameraMode == Enum.CameraMode.LockFirstPerson) then return true end return ((cg and (((j.CFrame.Position - cg.Position)).Magnitude <= 1.5)) or false) end local function ch() if p then if q then r = ce() f.MouseBehavior = ((r and o) or Enum.MouseBehavior.Default) else f.MouseBehavior = o end o = nil p = false q = false end end local function ci() l = false k = nil t = nil u = nil s = false ch() n = nil end local function cj() return (math.random() * math.clamp((tonumber(h.TriggerbotDelay) or 0), 0, 0.3)) end local function ck() t = nil u = nil end local function cl(cm) local cn = cm while (cn and (cn ~= d)) do local co = Players:GetPlayerFromCharacter(cn) if co then return co end cn = cn.Parent end return nil end local function cp() local cq = v("mouse1press") local cr = v("mouse1release") if ((type(cq) ~= "function") or (type(cr) ~= "function")) then return false end local cs = pcall(cq) local ct = pcall(cr) return (cs and ct) end local function cu() if ((not h.Triggerbot or not l) or not i.Target) then ck() return end local cv = i.Target local cw = cl(cv) local cx = cw if ((cw and (cw ~= e)) and (cw ~= k)) then cx = k end local cy = (cx and cx.Character) local cz = (cx and g.GetHead(cx)) local da = ((cw and (cw ~= e)) and (cw ~= cx)) local db = ((((((cx and (cx ~= e)) and cy) and cz) and cv) and ((da or (((cv.Name == "Head") and cv:IsDescendantOf(cy)))))) and at(cx, false, (k == cx))) if not db then ck() return end if (t ~= cx) then t = cx u = (os.clock() + cj()) end if (u and (os.clock() >= u)) then if not cp() then ck() return end u = (os.clock() + cj()) end end local function dc() if (p or s) then return end o = (n or f.MouseBehavior) if (ce() and (o ~= Enum.MouseBehavior.LockCenter)) then o = Enum.MouseBehavior.LockCenter end q = ce() p = true r = false f.MouseBehavior = Enum.MouseBehavior.LockCenter end local function dd() local de = h local df = f:GetMouseLocation() y.Position = df y.Radius = de.Fov y.Visible = de.Enabled if de.RainbowFov then m = (((m + de.RainbowSpeed)) % 1) y.Color = Color3.fromHSV(m, 1, 1) elseif (k and l) then y.Color = de.TargetedFovColor else y.Color = de.FovColor end end local di = f.InputBegan:Connect(function(dg, dh) if (not h.Enabled or not by(dg)) then return end if (not ce() and (f.MouseBehavior ~= Enum.MouseBehavior.LockCenter)) then return end l = true n = f.MouseBehavior if (ce() and (n ~= Enum.MouseBehavior.LockCenter)) then n = Enum.MouseBehavior.LockCenter end s = false end) local dk = f.InputEnded:Connect(function(dj) if not by(dj) then return end ci() end) local dr = c.RenderStepped:Connect(function(dl) j = d.CurrentCamera if not j then return end dd() if (not h.Enabled or not l) then ci() if (r and not ce()) then f.MouseBehavior = Enum.MouseBehavior.Default r = false end else if not cb() then ci() return end if ((q and not ce()) and p) then ch() s = true r = false return end local dm = bs() local dn = bt(dm) if dn then dc() local dp = (math.clamp((tonumber(h.Smoothing) or 5), 1, 100) / 100) local dq = CFrame.new(j.CFrame.Position, dn) j.CFrame = j.CFrame:Lerp(dq, dp) cu() else k = nil ck() ch() end end end) local ds = {} function ds:Destroy() di:Disconnect() dk:Disconnect() dr:Disconnect() y.Visible = false y:Remove() ci() end return ds end return a