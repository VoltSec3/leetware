local Aimbot = {} function Aimbot.new(config) local Players = config.Players local RunService = config.RunService local Workspace = config.Workspace local Player = config.Player local UserInputService = config.UserInputService local Utils = config.Utils local Settings = config.Settings local Mouse = Player:GetMouse() local Camera = Workspace.CurrentCamera local CurrentTarget = nil local Aiming = false local Hue = 0 local AimStartMouseBehavior = nil local OriginalMouseBehavior = nil local MouseLockedByAimbot = false local LockStartedInFirstPerson = false local PendingFirstPersonUnlock = false local MouseLockDisabled = false local TriggerbotTarget = nil local TriggerbotNextFire = nil local Randomizer = Random.new() local Sqrt3 = math.sqrt(3) local Sqrt5 = math.sqrt(5) local DegPerUnit = 0.07 local LastDeltaTime = (1 / 60) local ReactionReadyAt = nil local SpiralDirection = 1 local FailActive = false local FailStart = 0 local FailMagnitude = 0 local FailSnapDuration = 0 local FailRecoverDuration = 0 local FailYawSign = 1 local FailPitchRatio = 0 local NextFailTime = nil local TremorTime = 0 local TremorX = 0 local TremorY = 0 local TremorTargetX = 0 local TremorTargetY = 0 local TremorNextRefresh = 0 local SmoothingScale = 1 local SmoothingScaleTarget = 1 local NextSmoothingRefresh = 0 local SmoothedVelocity = nil local VelocityTarget = nil local WindVelocityX = 0 local WindVelocityY = 0 local WindMaxSpeed = nil local BezierActive = false local BezierControl1X = 0 local BezierControl1Y = 0 local BezierControl2X = 0 local BezierControl2Y = 0 local BezierStartTime = 0 local BezierDuration = 0 local BezierLastX = 0 local BezierLastY = 0 local BezierNextReplan = nil local function GetGlobalFunction(name) local environment = (((type(getgenv) == "function") and getgenv()) or _G) return ((environment and environment[name]) or _G[name]) end local FovCircle = Drawing.new("Circle") FovCircle.Thickness = 2 FovCircle.Filled = false FovCircle.Visible = false local function IsTeamBlocked(target) local excludedTeams = (Settings.ExcludeTeams or {}) local teamName = Utils.GetTeamName(target) if excludedTeams[teamName] then return true end return (((Settings.TeamCheck and target.Team) and Player.Team) and (target.Team == Player.Team)) end local function GetTeamPriority(target) local prioritizedTeams = (Settings.PrioritizeTeams or {}) return ((prioritizedTeams[Utils.GetTeamName(target)] and 1) or 0) end local function HasValidHealth(target, ignoreMinimum) local humanoid = Utils.GetHumanoid(target) if (not humanoid or (humanoid.Health <= 0)) then return false end return ((ignoreMinimum or not Settings.HealthCheck) or (humanoid.Health >= Settings.MinHealth)) end local function IsFriendBlocked(target) if not Settings.FriendCheck then return false end return Utils.IsFriend(Player, target) end local IsBlocked IsBlocked = function(character, ignorePlayers) local head = Utils.GetCharacterPart(character, "Head") if not head then return true end local origin = Camera.CFrame.Position local direction = (head.Position - origin) local raycastParams = RaycastParams.new() local filter = {Player.Character} if ignorePlayers then for _, target in ipairs(Players:GetPlayers()) do if ((target ~= Player) and target.Character) then table.insert(filter, target.Character) end end end raycastParams.FilterDescendantsInstances = filter raycastParams.FilterType = Enum.RaycastFilterType.Blacklist local result = Workspace:Raycast(origin, direction, raycastParams) return ((result ~= nil) and not result.Instance:IsDescendantOf(character)) end local function IsTargetValid(target, requireFov, maintainLock, ignorePlayers) if (not target or (target == Player)) then return false end local character = target.Character local head = Utils.GetHead(target) if (not character or not head) then return false end local range = math.clamp((tonumber(Settings.Range) or 500), 1, 1500) if (((head.Position - Camera.CFrame.Position)).Magnitude > range) then return false end if IsTeamBlocked(target) then return false end if IsFriendBlocked(target) then return false end if not HasValidHealth(target, maintainLock) then return false end local screenPosition, visible = Camera:WorldToViewportPoint(head.Position) if (not maintainLock and (((screenPosition.Z <= 0) or not visible))) then return false end if (not maintainLock and requireFov) then local cursorPosition = UserInputService:GetMouseLocation() local targetPosition = Vector2.new(screenPosition.X, screenPosition.Y) if (((targetPosition - cursorPosition)).Magnitude > Settings.Fov) then return false end end if ((not maintainLock and Settings.WallCheck) and IsBlocked(character, ignorePlayers)) then return false end return true end local function GetTarget() local candidates = {} local cursorPosition = UserInputService:GetMouseLocation() local cameraPosition = Camera.CFrame.Position for _, target in ipairs(Players:GetPlayers()) do if IsTargetValid(target, true) then local head = target.Character.Head local screenPosition = Camera:WorldToViewportPoint(head.Position) local targetPosition = Vector2.new(screenPosition.X, screenPosition.Y) local cursorDistance = ((targetPosition - cursorPosition)).Magnitude local playerDistance = ((head.Position - cameraPosition)).Magnitude table.insert(candidates, {target = target, priority = GetTeamPriority(target), cursorDistance = cursorDistance, playerDistance = playerDistance}) end end if (#candidates == 0) then return nil end local targeting = (Settings.Targeting or "Normal") if (targeting == "Random") then return candidates[math.random(1, #candidates)].target end local selected = candidates[1] for index = 2, #candidates do local candidate = candidates[index] local isBetter = false if (targeting == "Closest") then isBetter = (candidate.playerDistance < selected.playerDistance) elseif (targeting == "Furthest") then isBetter = (candidate.playerDistance > selected.playerDistance) else isBetter = ((candidate.priority > selected.priority) or (((candidate.priority == selected.priority) and (((candidate.cursorDistance < selected.cursorDistance) or (((candidate.cursorDistance == selected.cursorDistance) and (candidate.playerDistance < selected.playerDistance)))))))) end if isBetter then selected = candidate end end return selected.target end local function GetLockedTarget() if not Settings.LockAim then CurrentTarget = GetTarget() return CurrentTarget end if (CurrentTarget and IsTargetValid(CurrentTarget, false, true)) then return CurrentTarget end CurrentTarget = GetTarget() return CurrentTarget end local function GetAimPosition(target, deltaTime) local character = (target and target.Character) local head = Utils.GetHead(target) local root = Utils.GetRootPart(target) if not head then return nil end if (not root or (Settings.Prediction <= 0)) then return head.Position end local velocity = root.AssemblyLinearVelocity if Settings.SmoothPrediction then if ((VelocityTarget ~= target) or not SmoothedVelocity) then SmoothedVelocity = Vector3.new(velocity.X, velocity.Y, velocity.Z) VelocityTarget = target else SmoothedVelocity = SmoothedVelocity:Lerp(Vector3.new(velocity.X, velocity.Y, velocity.Z), math.clamp((((deltaTime or ((1 / 60)))) * 12), 0, 1)) end velocity = SmoothedVelocity else SmoothedVelocity = nil VelocityTarget = nil end return (head.Position + ((velocity * Settings.Prediction))) end local function IsInputBound(input) local bind = Settings.Bind if (typeof(bind) == "EnumItem") then return ((input.KeyCode == bind) or (input.UserInputType == bind)) end if (type(bind) ~= "string") then return false end bind = string.upper(bind) bind = string.gsub(bind, "^ENUM%.KEYCODE%.", "") bind = string.gsub(bind, "^ENUM%.USERINPUTTYPE%.", "") if ((bind == "MB1") or (bind == "MOUSEBUTTON1")) then return (input.UserInputType == Enum.UserInputType.MouseButton1) end if ((bind == "MB2") or (bind == "MOUSEBUTTON2")) then return (input.UserInputType == Enum.UserInputType.MouseButton2) end return ((input.KeyCode.Name == bind) or (input.UserInputType.Name == bind)) end local function IsBindHeld() local bind = Settings.Bind if (typeof(bind) == "EnumItem") then if (bind.EnumType == Enum.KeyCode) then return UserInputService:IsKeyDown(bind) end return UserInputService:IsMouseButtonPressed(bind) end if (type(bind) ~= "string") then return false end bind = string.upper(bind) bind = string.gsub(bind, "^ENUM%.KEYCODE%.", "") bind = string.gsub(bind, "^ENUM%.USERINPUTTYPE%.", "") if ((bind == "MB1") or (bind == "MOUSEBUTTON1")) then return UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) end if ((bind == "MB2") or (bind == "MOUSEBUTTON2")) then return UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) end local keyCode = Enum.KeyCode[bind] if keyCode then return UserInputService:IsKeyDown(keyCode) end return false end local function IsFirstPerson() local character = Player.Character local head = (character and character:FindFirstChild("Head")) if (Player.CameraMode == Enum.CameraMode.LockFirstPerson) then return true end return ((head and (((Camera.CFrame.Position - head.Position)).Magnitude <= 1.5)) or false) end local function UnlockMouse() if MouseLockedByAimbot then if LockStartedInFirstPerson then PendingFirstPersonUnlock = IsFirstPerson() UserInputService.MouseBehavior = ((PendingFirstPersonUnlock and OriginalMouseBehavior) or Enum.MouseBehavior.Default) else UserInputService.MouseBehavior = OriginalMouseBehavior end OriginalMouseBehavior = nil MouseLockedByAimbot = false LockStartedInFirstPerson = false end end local function StopAiming() Aiming = false CurrentTarget = nil TriggerbotTarget = nil TriggerbotNextFire = nil MouseLockDisabled = false ReactionReadyAt = nil NextFailTime = nil FailActive = false SmoothedVelocity = nil VelocityTarget = nil WindVelocityX = 0 WindVelocityY = 0 WindMaxSpeed = nil BezierActive = false BezierNextReplan = nil SmoothingScale = 1 SmoothingScaleTarget = 1 NextSmoothingRefresh = 0 TremorNextRefresh = 0 UnlockMouse() AimStartMouseBehavior = nil end local function GetTriggerbotDelay() return math.clamp((tonumber(Settings.TriggerbotDelay) or 0), 0, 0.3) end local function ResetTriggerbot() TriggerbotTarget = nil TriggerbotNextFire = nil end local function ClickMouse1() local press = GetGlobalFunction("mouse1press") local release = GetGlobalFunction("mouse1release") if ((type(press) ~= "function") or (type(release) ~= "function")) then return false end local pressed = pcall(press) local released = pcall(release) return (pressed and released) end local function GetHeadCursorRadius(head) local headPosition, visible = Camera:WorldToViewportPoint(head.Position) if (not visible or (headPosition.Z <= 0)) then return nil end local edgePosition = Camera:WorldToViewportPoint((head.Position + Vector3.new((head.Size.X / 2), 0, 0))) local radius = ((Vector2.new(edgePosition.X, edgePosition.Y) - Vector2.new(headPosition.X, headPosition.Y))).Magnitude return math.max((radius * 1.25), 6) end local function GetTriggerbotTarget() local cursorPosition = UserInputService:GetMouseLocation() local nearestTarget = nil local nearestDistance = math.huge for _, target in ipairs(Players:GetPlayers()) do local head = Utils.GetHead(target) if (head and IsTargetValid(target, false, false, true)) then local screenPosition, visible = Camera:WorldToViewportPoint(head.Position) local cursorDistance = ((Vector2.new(screenPosition.X, screenPosition.Y) - cursorPosition)).Magnitude local cursorRadius = GetHeadCursorRadius(head) if (((visible and cursorRadius) and (cursorDistance <= cursorRadius)) and (cursorDistance < nearestDistance)) then nearestTarget = target nearestDistance = cursorDistance end end end return nearestTarget end local function UpdateTriggerbot() if (not Settings.Triggerbot or not Aiming) then ResetTriggerbot() return end local targetPlayer = GetTriggerbotTarget() local isValidHead = ((targetPlayer and (targetPlayer ~= Player)) and IsTargetValid(targetPlayer, false, false, true)) if not isValidHead then ResetTriggerbot() return end local now = os.clock() if (TriggerbotTarget ~= targetPlayer) then TriggerbotTarget = targetPlayer TriggerbotNextFire = (now + GetTriggerbotDelay()) end if (TriggerbotNextFire and (now >= TriggerbotNextFire)) then if not ClickMouse1() then ResetTriggerbot() return end TriggerbotNextFire = now end end local function GetNumber(name, default) local value = tonumber(Settings[name]) return (value or default) end local function MinJerkCurve(t) t = math.clamp(t, 0, 1) return (((10 * (t ^ 3)) - (15 * (t ^ 4))) + (6 * (t ^ 5))) end local function GetAngleDelta(targetCFrame) local relative = (targetCFrame * Camera.CFrame.Rotation:Inverse()) local pitch, yaw = relative:ToEulerAnglesYXZ() return math.deg(yaw), math.deg(pitch) end local function OffsetAimPosition(position, yawDegrees, pitchDegrees) if ((yawDegrees == 0) and (pitchDegrees == 0)) then return position end local origin = Camera.CFrame.Position local displacement = (position - origin) local distance = displacement.Magnitude if (distance <= 1e-6) then return position end local rotation = (CFrame.fromAxisAngle(Camera.CFrame.UpVector, math.rad(yawDegrees)) * CFrame.fromAxisAngle(Camera.CFrame.RightVector, math.rad(pitchDegrees))) return (origin + (rotation:VectorToWorldSpace(displacement.Unit) * distance)) end local function UpdateDynamicSmoothing(now) if not Settings.DynamicSmoothing then SmoothingScale = 1 SmoothingScaleTarget = 1 return 1 end local variation = (math.clamp(GetNumber("SmoothingVariation", 25), 0, 90) / 100) if (now >= NextSmoothingRefresh) then SmoothingScaleTarget = (1 + Randomizer:NextNumber(-variation, variation)) NextSmoothingRefresh = (now + Randomizer:NextNumber(0.15, 0.4)) end SmoothingScale = (SmoothingScale + (((SmoothingScaleTarget - SmoothingScale)) * math.clamp((LastDeltaTime * 8), 0, 1))) return math.clamp(SmoothingScale, 0.4, 2) end local function UpdateTremor(now) if not Settings.Tremor then TremorX = 0 TremorY = 0 return 0, 0 end if (now >= TremorNextRefresh) then TremorTargetX = Randomizer:NextNumber(-1, 1) TremorTargetY = Randomizer:NextNumber(-1, 1) TremorNextRefresh = (now + Randomizer:NextNumber(0.08, 0.22)) end TremorX = (TremorX + (((TremorTargetX - TremorX)) * math.clamp((LastDeltaTime * 14), 0, 1))) TremorY = (TremorY + (((TremorTargetY - TremorY)) * math.clamp((LastDeltaTime * 14), 0, 1))) TremorTime = (TremorTime + LastDeltaTime) local amplitude = math.max(GetNumber("TremorAmount", 0.25), 0) local sineX = math.sin((((TremorTime * math.pi) * 2) * 8.7)) local sineY = math.sin(((((TremorTime * math.pi) * 2) * 11.3) + 1.3)) return (amplitude * ((TremorX + (sineX * 0.25)))), (amplitude * ((TremorY + (sineY * 0.25)))) end local function UpdateTrackingFail(target, now) if not Settings.TrackingFail then FailActive = false NextFailTime = nil return 0, 0 end local minInterval = math.max(GetNumber("FailMinInterval", 4), 0.5) local maxInterval = math.max(GetNumber("FailMaxInterval", 9), minInterval) if not FailActive then if not NextFailTime then NextFailTime = (now + Randomizer:NextNumber(minInterval, maxInterval)) return 0, 0 end if (now < NextFailTime) then return 0, 0 end local sign = 0 local root = Utils.GetRootPart(target) if root then local lateral = root.AssemblyLinearVelocity:Dot(Camera.CFrame.RightVector) if (math.abs(lateral) > 2) then sign = (((lateral > 0) and 1) or -1) end end if (sign == 0) then sign = (((Randomizer:NextInteger(0, 1) == 0) and 1) or -1) end FailActive = true FailStart = now FailMagnitude = (Randomizer:NextNumber(0.35, 1) * math.max(GetNumber("FailMaxDegrees", 5), 0.5)) FailSnapDuration = (math.max(GetNumber("FailSnapTime", 0.09), 0.02) * Randomizer:NextNumber(0.7, 1.3)) FailRecoverDuration = (math.max(GetNumber("FailRecoveryTime", 0.85), 0.1) * Randomizer:NextNumber(0.8, 1.4)) FailYawSign = sign FailPitchRatio = Randomizer:NextNumber(-0.35, 0.35) NextFailTime = nil end local elapsed = (now - FailStart) local offset if (elapsed < FailSnapDuration) then offset = (FailMagnitude * MinJerkCurve((elapsed / FailSnapDuration))) else local progress = (((elapsed - FailSnapDuration)) / FailRecoverDuration) if (progress >= 1) then FailActive = false NextFailTime = (now + Randomizer:NextNumber(minInterval, maxInterval)) return 0, 0 end offset = (FailMagnitude * ((1 - MinJerkCurve(progress)))) end return (offset * FailYawSign), (offset * FailPitchRatio) end local function LinearStep(deltaYaw, deltaPitch, scale) local base = (math.clamp((tonumber(Settings.Smoothing) or 5), 1, 100) / 100) local alpha = math.clamp((1 - (((1 - base)) ^ ((LastDeltaTime * 60)))), 0, 1) alpha = math.clamp((alpha * scale), 0, 1) return (deltaYaw * alpha), (deltaPitch * alpha) end local function SpiralStep(deltaYaw, deltaPitch, scale) local smoothness = math.clamp((tonumber(Settings.Smoothing) or 5), 1, 100) local fraction = (1 - (((1 - (1 / smoothness))) ^ ((LastDeltaTime * 60)))) local xScale = math.max(GetNumber("SpiralXScale", 6), 0.5) local yScale = math.max(GetNumber("SpiralYScale", 9), 0.5) return ((((deltaYaw + ((SpiralDirection * deltaPitch) / xScale))) * fraction) * scale), ((((deltaPitch + ((SpiralDirection * deltaYaw) / yScale))) * fraction) * scale) end local function WindMouseStep(deltaYaw, deltaPitch, scale) local gravity = (GetNumber("WindGravity", 9) * DegPerUnit) local windBase = (GetNumber("WindStrength", 3) * DegPerUnit) local speedBase = (GetNumber("WindMaxSpeed", 15) * DegPerUnit) local dampDistance = (GetNumber("WindDampDistance", 12) * DegPerUnit) local distance = math.sqrt(((deltaYaw * deltaYaw) + (deltaPitch * deltaPitch))) if (distance < 0.005) then WindVelocityX = 0 WindVelocityY = 0 return 0, 0 end WindMaxSpeed = (WindMaxSpeed or speedBase) if (distance >= dampDistance) then local windMag = math.min(windBase, distance) WindVelocityX = ((WindVelocityX / Sqrt3) + ((Randomizer:NextNumber(-1, 1) * windMag) / Sqrt5)) WindVelocityY = ((WindVelocityY / Sqrt3) + ((Randomizer:NextNumber(-1, 1) * windMag) / Sqrt5)) WindMaxSpeed = math.min((WindMaxSpeed * Sqrt5), speedBase) else WindVelocityX /= Sqrt3 WindVelocityY /= Sqrt3 if (WindMaxSpeed < (3 * DegPerUnit)) then WindMaxSpeed = (((Randomizer:NextNumber() * 3) * DegPerUnit) + (3 * DegPerUnit)) else WindMaxSpeed /= Sqrt5 end end WindVelocityX += (gravity * ((deltaYaw / distance))) WindVelocityY += (gravity * ((deltaPitch / distance))) local speed = math.sqrt(((WindVelocityX * WindVelocityX) + (WindVelocityY * WindVelocityY))) local limit = (math.min(WindMaxSpeed, speedBase) * math.clamp(scale, 0.4, 2)) if (speed > limit) then local clip = ((limit / 2) + ((Randomizer:NextNumber() * limit) / 2)) WindVelocityX *= (clip / speed) WindVelocityY *= (clip / speed) end local normalizedTime = math.clamp((LastDeltaTime * 60), 0.1, 3) return (WindVelocityX * normalizedTime), (WindVelocityY * normalizedTime) end local function BezierStep(deltaYaw, deltaPitch, distance, now) if not BezierActive then local replanDistance = math.max(GetNumber("BezierReplanDistance", 3), 0.1) if (BezierNextReplan and (now < BezierNextReplan)) then return LinearStep(deltaYaw, deltaPitch, 1) end if (distance < replanDistance) then return LinearStep(deltaYaw, deltaPitch, 1) end local curvature = (math.clamp(GetNumber("BezierCurvature", 35), 0, 100) / 100) local direction = (((Randomizer:NextInteger(0, 1) == 0) and 1) or -1) local perpendicularX = (-deltaPitch / distance) local perpendicularY = (deltaYaw / distance) local offset1 = (((distance * curvature) * Randomizer:NextNumber(0.4, 1)) * direction) local offset2 = ((offset1 * Randomizer:NextNumber(0.3, 0.8)) * ((((Randomizer:NextInteger(0, 1) == 0) and 1) or -1))) BezierControl1X = ((deltaYaw * 0.3) + (perpendicularX * offset1)) BezierControl1Y = ((deltaPitch * 0.3) + (perpendicularY * offset1)) BezierControl2X = ((deltaYaw * 0.7) + (perpendicularX * offset2)) BezierControl2Y = ((deltaPitch * 0.7) + (perpendicularY * offset2)) BezierDuration = math.clamp((GetNumber("BezierDuration", 0.35) * math.clamp((distance / 15), 0.6, 1.6)), 0.05, 5) BezierStartTime = now BezierLastX = 0 BezierLastY = 0 BezierActive = true end local progress = (((now - BezierStartTime)) / BezierDuration) if (progress >= 1) then BezierActive = false BezierNextReplan = (now + Randomizer:NextNumber(0.4, 1.1)) return LinearStep(deltaYaw, deltaPitch, 1) end local eased = MinJerkCurve(progress) local inverse = (1 - eased) local pointX = ((((((3 * inverse) * inverse) * eased) * BezierControl1X) + ((((3 * inverse) * eased) * eased) * BezierControl2X)) + (((eased * eased) * eased) * deltaYaw)) local pointY = ((((((3 * inverse) * inverse) * eased) * BezierControl1Y) + ((((3 * inverse) * eased) * eased) * BezierControl2Y)) + (((eased * eased) * eased) * deltaPitch)) local stepX = (pointX - BezierLastX) local stepY = (pointY - BezierLastY) BezierLastX = pointX BezierLastY = pointY return stepX, stepY end local function ComputeAimStep(style, deltaYaw, deltaPitch, distance, now, scale) if (style == "WindMouse") then return WindMouseStep(deltaYaw, deltaPitch, scale) end if (style == "Bezier") then return BezierStep(deltaYaw, deltaPitch, distance, now) end if (style == "Spiral") then return SpiralStep(deltaYaw, deltaPitch, scale) end return LinearStep(deltaYaw, deltaPitch, scale) end local function LockMouse() if (MouseLockedByAimbot or MouseLockDisabled) then return end OriginalMouseBehavior = (AimStartMouseBehavior or UserInputService.MouseBehavior) if (IsFirstPerson() and (OriginalMouseBehavior ~= Enum.MouseBehavior.LockCenter)) then OriginalMouseBehavior = Enum.MouseBehavior.LockCenter end LockStartedInFirstPerson = IsFirstPerson() MouseLockedByAimbot = true PendingFirstPersonUnlock = false UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter end local function UpdateCircle() local settings = Settings local mousePosition = UserInputService:GetMouseLocation() FovCircle.Position = mousePosition FovCircle.Radius = settings.Fov FovCircle.Visible = settings.Enabled if settings.RainbowFov then Hue = (((Hue + settings.RainbowSpeed)) % 1) FovCircle.Color = Color3.fromHSV(Hue, 1, 1) elseif (CurrentTarget and Aiming) then FovCircle.Color = settings.TargetedFovColor else FovCircle.Color = settings.FovColor end end local inputBegan = UserInputService.InputBegan:Connect(function(input, processed) if (not Settings.Enabled or not IsInputBound(input)) then return end if (not IsFirstPerson() and (UserInputService.MouseBehavior ~= Enum.MouseBehavior.LockCenter)) then return end Aiming = true AimStartMouseBehavior = UserInputService.MouseBehavior if (IsFirstPerson() and (AimStartMouseBehavior ~= Enum.MouseBehavior.LockCenter)) then AimStartMouseBehavior = Enum.MouseBehavior.LockCenter end MouseLockDisabled = false SpiralDirection = (((Randomizer:NextInteger(0, 1) == 0) and 1) or -1) if Settings.ReactionTime then local minDelay = math.max(GetNumber("ReactionTimeMin", 0.08), 0) local maxDelay = math.max(GetNumber("ReactionTimeMax", 0.2), minDelay) ReactionReadyAt = (os.clock() + Randomizer:NextNumber(minDelay, maxDelay)) else ReactionReadyAt = nil end end) local inputEnded = UserInputService.InputEnded:Connect(function(input) if not IsInputBound(input) then return end StopAiming() end) local renderConnection = RunService.RenderStepped:Connect(function(deltaTime) Camera = Workspace.CurrentCamera if not Camera then return end UpdateCircle() if (not Settings.Enabled or not Aiming) then StopAiming() if (PendingFirstPersonUnlock and not IsFirstPerson()) then UserInputService.MouseBehavior = Enum.MouseBehavior.Default PendingFirstPersonUnlock = false end else if not IsBindHeld() then StopAiming() return end if ((LockStartedInFirstPerson and not IsFirstPerson()) and MouseLockedByAimbot) then UnlockMouse() MouseLockDisabled = true PendingFirstPersonUnlock = false return end local now = os.clock() local target = GetLockedTarget() local aimPosition = GetAimPosition(target, deltaTime) if not aimPosition then CurrentTarget = nil UnlockMouse() UpdateTriggerbot() return end LockMouse() if (ReactionReadyAt and (now < ReactionReadyAt)) then UpdateTriggerbot() return end LastDeltaTime = math.clamp(deltaTime, 0.001, 0.1) CurrentTarget = target local failYaw, failPitch = UpdateTrackingFail(target, now) local tremorYaw, tremorPitch = UpdateTremor(now) aimPosition = OffsetAimPosition(aimPosition, (failYaw + tremorYaw), (failPitch + tremorPitch)) local targetCFrame = CFrame.new(Camera.CFrame.Position, aimPosition) local deltaYaw, deltaPitch = GetAngleDelta(targetCFrame) local distance = math.sqrt(((deltaYaw * deltaYaw) + (deltaPitch * deltaPitch))) local smoothingScale = UpdateDynamicSmoothing(now) local stepYaw, stepPitch = ComputeAimStep((Settings.AimStyle or "Linear"), deltaYaw, deltaPitch, distance, now, smoothingScale) Camera.CFrame = (Camera.CFrame * CFrame.fromEulerAnglesYXZ(math.rad(stepPitch), math.rad(stepYaw), 0)) UpdateTriggerbot() end end) local controller = {} function controller:Destroy() inputBegan:Disconnect() inputEnded:Disconnect() renderConnection:Disconnect() FovCircle.Visible = false FovCircle:Remove() StopAiming() end return controller end return Aimbot