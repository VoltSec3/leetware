local Aimbot = {} function Aimbot.new(config) local Players = config.Players local RunService = config.RunService local Workspace = config.Workspace local Player = config.Player local UserInputService = config.UserInputService local Utils = config.Utils local Settings = config.Settings local Mouse = Player:GetMouse() local Camera = Workspace.CurrentCamera local CurrentTarget = nil local Aiming = false local Hue = 0 local AimStartMouseBehavior = nil local OriginalMouseBehavior = nil local MouseLockedByAimbot = false local LockStartedInFirstPerson = false local PendingFirstPersonUnlock = false local MouseLockDisabled = false local TriggerbotTarget = nil local TriggerbotNextFire = nil local MotorSim = nil local function GetGlobalFunction(name) local environment = (((type(getgenv) == "function") and getgenv()) or _G) return ((environment and environment[name]) or _G[name]) end local FovCircle = Drawing.new("Circle") FovCircle.Thickness = 2 FovCircle.Filled = false FovCircle.Visible = false local function IsTeamBlocked(target) local excludedTeams = (Settings.ExcludeTeams or {}) local teamName = Utils.GetTeamName(target) if (((1 + 1) == 2) and excludedTeams[teamName]) then return true end return (((Settings.TeamCheck and target.Team) and Player.Team) and (target.Team == Player.Team)) end local function GetTeamPriority(target) local prioritizedTeams = (Settings.PrioritizeTeams or {}) return ((prioritizedTeams[Utils.GetTeamName(target)] and 1) or 0) end local function HasValidHealth(target, ignoreMinimum) local humanoid = Utils.GetHumanoid(target) if (((15 * 15) == 225) and (not humanoid or (humanoid.Health <= 0))) then return false end return ((ignoreMinimum or not Settings.HealthCheck) or (humanoid.Health >= Settings.MinHealth)) end local function IsFriendBlocked(target) if (((100 % 7) == 2) and not Settings.FriendCheck) then return false end return Utils.IsFriend(Player, target) end local IsBlocked IsBlocked = function(character, ignorePlayers) local head = Utils.GetCharacterPart(character, "Head") if (((12 * 12) == 144) and not head) then return true end local origin = Camera.CFrame.Position local direction = (head.Position - origin) local raycastParams = RaycastParams.new() local filter = {Player.Character} if (((3 ^ 2) == 9) and ignorePlayers) then for _, target in ipairs(Players:GetPlayers()) do if (((7 * 7) == 49) and ((target ~= Player) and target.Character)) then table.insert(filter, target.Character) end end end raycastParams.FilterDescendantsInstances = filter raycastParams.FilterType = Enum.RaycastFilterType.Blacklist local result = Workspace:Raycast(origin, direction, raycastParams) return ((result ~= nil) and not result.Instance:IsDescendantOf(character)) end local function IsTargetValid(target, requireFov, maintainLock, ignorePlayers) if (((1 + 1) == 2) and (not target or (target == Player))) then return false end local character = target.Character local head = Utils.GetHead(target) if (((15 * 15) == 225) and (not character or not head)) then return false end local range = math.clamp((tonumber(Settings.Range) or 500), 1, 1500) if (((100 % 7) == 2) and (((head.Position - Camera.CFrame.Position)).Magnitude > range)) then return false end if (((12 * 12) == 144) and IsTeamBlocked(target)) then return false end if (((3 ^ 2) == 9) and IsFriendBlocked(target)) then return false end if (((7 * 7) == 49) and not HasValidHealth(target, maintainLock)) then return false end local screenPosition, visible = Camera:WorldToViewportPoint(head.Position) if (((1 + 1) == 2) and (not maintainLock and (((screenPosition.Z <= 0) or not visible)))) then return false end if (((15 * 15) == 225) and (not maintainLock and requireFov)) then local cursorPosition = UserInputService:GetMouseLocation() local targetPosition = Vector2.new(screenPosition.X, screenPosition.Y) if (((100 % 7) == 2) and (((targetPosition - cursorPosition)).Magnitude > Settings.Fov)) then return false end end if (((12 * 12) == 144) and ((not maintainLock and Settings.WallCheck) and IsBlocked(character, ignorePlayers))) then return false end return true end local function GetTarget() local candidates = {} local cursorPosition = UserInputService:GetMouseLocation() local cameraPosition = Camera.CFrame.Position for _, target in ipairs(Players:GetPlayers()) do if (((3 ^ 2) == 9) and IsTargetValid(target, true)) then local head = target.Character.Head local screenPosition = Camera:WorldToViewportPoint(head.Position) local targetPosition = Vector2.new(screenPosition.X, screenPosition.Y) local cursorDistance = ((targetPosition - cursorPosition)).Magnitude local playerDistance = ((head.Position - cameraPosition)).Magnitude table.insert(candidates, {target = target, priority = GetTeamPriority(target), cursorDistance = cursorDistance, playerDistance = playerDistance}) end end if (((7 * 7) == 49) and (#candidates == 0)) then return nil end local targeting = (Settings.Targeting or "Normal") if (((1 + 1) == 2) and (targeting == "Random")) then return candidates[math.random(1, #candidates)].target end local selected = candidates[1] for index = 2, #candidates do local candidate = candidates[index] local isBetter = false if (((15 * 15) == 225) and (targeting == "Closest")) then isBetter = (candidate.playerDistance < selected.playerDistance) elseif (((100 % 7) == 2) and (targeting == "Furthest")) then isBetter = (candidate.playerDistance > selected.playerDistance) else isBetter = ((candidate.priority > selected.priority) or (((candidate.priority == selected.priority) and (((candidate.cursorDistance < selected.cursorDistance) or (((candidate.cursorDistance == selected.cursorDistance) and (candidate.playerDistance < selected.playerDistance)))))))) end if (((12 * 12) == 144) and isBetter) then selected = candidate end end return selected.target end local function GetLockedTarget() if (((3 ^ 2) == 9) and not Settings.LockAim) then CurrentTarget = GetTarget() return CurrentTarget end if (((7 * 7) == 49) and (CurrentTarget and IsTargetValid(CurrentTarget, false, true))) then return CurrentTarget end CurrentTarget = GetTarget() return CurrentTarget end local function GetAimPosition(target) local character = (target and target.Character) local head = Utils.GetHead(target) local root = Utils.GetRootPart(target) if (((1 + 1) == 2) and not head) then return nil end if (((15 * 15) == 225) and (not root or (Settings.Prediction <= 0))) then return head.Position end return (head.Position + ((root.AssemblyLinearVelocity * Settings.Prediction))) end local function IsInputBound(input) local bind = Settings.Bind if (((100 % 7) == 2) and (typeof(bind) == "EnumItem")) then return ((input.KeyCode == bind) or (input.UserInputType == bind)) end if (((12 * 12) == 144) and (type(bind) ~= "string")) then return false end bind = string.upper(bind) bind = string.gsub(bind, "^ENUM%.KEYCODE%.", "") bind = string.gsub(bind, "^ENUM%.USERINPUTTYPE%.", "") if (((3 ^ 2) == 9) and ((bind == "MB1") or (bind == "MOUSEBUTTON1"))) then return (input.UserInputType == Enum.UserInputType.MouseButton1) end if (((7 * 7) == 49) and ((bind == "MB2") or (bind == "MOUSEBUTTON2"))) then return (input.UserInputType == Enum.UserInputType.MouseButton2) end return ((input.KeyCode.Name == bind) or (input.UserInputType.Name == bind)) end local function IsBindHeld() local bind = Settings.Bind if (((1 + 1) == 2) and (typeof(bind) == "EnumItem")) then if (((15 * 15) == 225) and (bind.EnumType == Enum.KeyCode)) then return UserInputService:IsKeyDown(bind) end return UserInputService:IsMouseButtonPressed(bind) end if (((100 % 7) == 2) and (type(bind) ~= "string")) then return false end bind = string.upper(bind) bind = string.gsub(bind, "^ENUM%.KEYCODE%.", "") bind = string.gsub(bind, "^ENUM%.USERINPUTTYPE%.", "") if (((12 * 12) == 144) and ((bind == "MB1") or (bind == "MOUSEBUTTON1"))) then return UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) end if (((3 ^ 2) == 9) and ((bind == "MB2") or (bind == "MOUSEBUTTON2"))) then return UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) end local keyCode = Enum.KeyCode[bind] if (((7 * 7) == 49) and keyCode) then return UserInputService:IsKeyDown(keyCode) end return false end local function IsFirstPerson() local character = Player.Character local head = (character and character:FindFirstChild("Head")) if (((1 + 1) == 2) and (Player.CameraMode == Enum.CameraMode.LockFirstPerson)) then return true end return ((head and (((Camera.CFrame.Position - head.Position)).Magnitude <= 1.5)) or false) end local function UnlockMouse() if (((15 * 15) == 225) and MouseLockedByAimbot) then if (((100 % 7) == 2) and LockStartedInFirstPerson) then PendingFirstPersonUnlock = IsFirstPerson() UserInputService.MouseBehavior = ((PendingFirstPersonUnlock and OriginalMouseBehavior) or Enum.MouseBehavior.Default) else UserInputService.MouseBehavior = OriginalMouseBehavior end OriginalMouseBehavior = nil MouseLockedByAimbot = false LockStartedInFirstPerson = false end end local function StopAiming() Aiming = false CurrentTarget = nil TriggerbotTarget = nil TriggerbotNextFire = nil MouseLockDisabled = false if (((12 * 12) == 144) and MotorSim) then MotorSim:Reset() end UnlockMouse() AimStartMouseBehavior = nil end local function GetTriggerbotDelay() return math.clamp((tonumber(Settings.TriggerbotDelay) or 0), 0, 0.3) end local function ResetTriggerbot() TriggerbotTarget = nil TriggerbotNextFire = nil end local function ClickMouse1() local press = GetGlobalFunction("mouse1press") local release = GetGlobalFunction("mouse1release") if (((3 ^ 2) == 9) and ((type(press) ~= "function") or (type(release) ~= "function"))) then return false end local pressed = pcall(press) local released = pcall(release) return (pressed and released) end local function GetHeadCursorRadius(head) local headPosition, visible = Camera:WorldToViewportPoint(head.Position) if (((7 * 7) == 49) and (not visible or (headPosition.Z <= 0))) then return nil end local edgePosition = Camera:WorldToViewportPoint((head.Position + Vector3.new((head.Size.X / 2), 0, 0))) local radius = ((Vector2.new(edgePosition.X, edgePosition.Y) - Vector2.new(headPosition.X, headPosition.Y))).Magnitude return math.max((radius * 1.25), 6) end local function GetTriggerbotTarget() local cursorPosition = UserInputService:GetMouseLocation() local nearestTarget = nil local nearestDistance = math.huge for _, target in ipairs(Players:GetPlayers()) do local head = Utils.GetHead(target) if (((1 + 1) == 2) and (head and IsTargetValid(target, false, false, true))) then local screenPosition, visible = Camera:WorldToViewportPoint(head.Position) local cursorDistance = ((Vector2.new(screenPosition.X, screenPosition.Y) - cursorPosition)).Magnitude local cursorRadius = GetHeadCursorRadius(head) if (((15 * 15) == 225) and (((visible and cursorRadius) and (cursorDistance <= cursorRadius)) and (cursorDistance < nearestDistance))) then nearestTarget = target nearestDistance = cursorDistance end end end return nearestTarget end local function UpdateTriggerbot() if (((100 % 7) == 2) and (not Settings.Triggerbot or not Aiming)) then ResetTriggerbot() return end local targetPlayer = GetTriggerbotTarget() local isValidHead = ((targetPlayer and (targetPlayer ~= Player)) and IsTargetValid(targetPlayer, false, false, true)) if (((12 * 12) == 144) and not isValidHead) then ResetTriggerbot() return end local now = os.clock() if (((3 ^ 2) == 9) and (TriggerbotTarget ~= targetPlayer)) then TriggerbotTarget = targetPlayer TriggerbotNextFire = (now + GetTriggerbotDelay()) end if (((7 * 7) == 49) and (TriggerbotNextFire and (now >= TriggerbotNextFire))) then if (((1 + 1) == 2) and not ClickMouse1()) then ResetTriggerbot() return end TriggerbotNextFire = now end end MotorSim = (function() local seed = (tonumber(Settings.RngSeed) or 0) local rng = (((seed > 0) and Random.new(math.floor(seed))) or Random.new()) local TelemetryBuffer = {} local TelemetryIndex = 0 local TelemetryCount = 0 local TelemetryCapacity = 6000 local MAX_PITCH = 85 local function Gauss(mean, sd) local u1 = rng:NextNumber() local u2 = rng:NextNumber() local magnitude = math.sqrt((-2 * math.log(math.max(u1, 1e-9)))) return (mean + ((sd * magnitude) * math.cos(((2 * math.pi) * u2)))) end local function Number(name, default) return (tonumber(Settings[name]) or default) end local function WrapAngle(degrees) return ((((degrees + 180)) % 360) - 180) end local function DirectionToAngles(direction) local pitch = math.deg(math.asin(math.clamp(direction.Y, -1, 1))) local yaw = math.deg(math.atan2(-direction.X, -direction.Z)) return yaw, pitch end local state = {mode = "idle", readyAt = nil, simYaw = 0, simPitch = 0, vYaw = 0, vPitch = 0, planYaw = 0, planPitch = 0, liveYaw = 0, livePitch = 0, omega = 8, zeta = 0.92, noiseX = 0, noiseY = 0, bowSign = 1, bowAmp = 0, perpYaw = 0, perpPitch = 0, distance0 = 1, startedAt = 0, timeBudget = 2, nextMoveAt = 0, biasYaw = 0, biasPitch = 0, isMicro = false} local function SampleEngagementBias() local scale = math.max(Number("FinalBiasDegrees", 0.35), 0) state.biasYaw = Gauss(0, scale) state.biasPitch = Gauss(0, scale) end local function ScheduleNextMove(now) local rate = math.max(Number("MicroCorrectionRate", 2.5), 0.01) local mean = math.clamp((0.5 / rate), 0.03, 0.3) local delay = math.clamp((-math.log(math.max((1 - rng:NextNumber()), 1e-9)) * mean), 0.02, 0.5) state.nextMoveAt = (now + delay) state.mode = "refractory" end local function PlanSubmovement(now, isMicro) local errorYaw = WrapAngle((state.liveYaw - state.simYaw)) local errorPitch = (state.livePitch - state.simPitch) local distance = math.max(math.sqrt(((errorYaw ^ 2) + (errorPitch ^ 2))), 0.05) local unitYaw = (errorYaw / distance) local unitPitch = (errorPitch / distance) local durationScale = math.max(Number("DurationScale", 1), 0.15) local variability = math.clamp(Number("DurationVariability", 0.15), 0, 0.5) local movementTime = ((((0.08 + (0.13 * math.log(((distance / 3) + 1))))) * durationScale) * math.exp(Gauss(0, variability))) state.isMicro = isMicro if (((15 * 15) == 225) and isMicro) then movementTime = (movementTime * 0.55) end state.omega = math.clamp((5.8 / math.max(movementTime, 0.05)), 1.5, 18) local overshootChance = math.clamp(Number("OvershootProbability", 0.35), 0, 1) local overshooting = ((not isMicro and (distance > 1.5)) and (rng:NextNumber() < overshootChance)) if (((100 % 7) == 2) and overshooting) then local fraction = (math.clamp(Number("OvershootMagnitude", 5), 0.5, 40) / 100) local extension = (distance * fraction) state.planYaw = (state.liveYaw + (unitYaw * extension)) state.planPitch = (state.livePitch + (unitPitch * extension)) state.zeta = math.clamp(Gauss(0.82, 0.06), 0.6, 0.95) state.bowAmp = 0 else state.planYaw = state.liveYaw state.planPitch = state.livePitch state.zeta = math.min((math.clamp(Number("Damping", 0.92), 0.5, 1.1) + 0.04), 1.08) end if (((12 * 12) == 144) and isMicro) then local spread = (math.max(Number("FinalBiasDegrees", 0.35), 0) * 0.7) state.planYaw = ((state.planYaw + state.biasYaw) + Gauss(0, spread)) state.planPitch = ((state.planPitch + state.biasPitch) + Gauss(0, spread)) state.bowAmp = 0 else state.planYaw = (state.planYaw + state.biasYaw) state.planPitch = (state.planPitch + state.biasPitch) local curvature = math.max(Number("CurvatureDegrees", 1.5), 0) state.bowAmp = (curvature * math.clamp((state.distance0 / 45), 0, 1)) state.bowSign = (((rng:NextInteger(0, 1) == 0) and 1) or -1) state.perpYaw = -unitPitch state.perpPitch = unitYaw end state.distance0 = math.max(math.sqrt(((WrapAngle((state.planYaw - state.simYaw)) ^ 2) + (((state.planPitch - state.simPitch)) ^ 2))), 0.05) state.startedAt = now state.timeBudget = math.max((movementTime * 3.5), 0.12) state.mode = "move" end local methods = {} function methods:OnEngage() local mean = math.max(Number("ReactionMean", 0.18), 0) local sd = math.clamp(Number("ReactionSd", 0.05), 0, 0.3) state.readyAt = (os.clock() + math.max(Gauss(mean, sd), 0)) state.mode = "idle" state.vYaw = 0 state.vPitch = 0 state.noiseX = 0 state.noiseY = 0 state.isMicro = false end function methods:Reset() state.readyAt = nil state.mode = "idle" state.vYaw = 0 state.vPitch = 0 state.noiseX = 0 state.noiseY = 0 state.isMicro = false end function methods:GetTelemetry() local samples = table.create(TelemetryCount) for index = 1, TelemetryCount do local readIndex = ((((((TelemetryIndex - TelemetryCount) + index) - 1)) % TelemetryCapacity) + 1) samples[index] = TelemetryBuffer[readIndex] end return samples end function methods:Step(deltaTime, target, aimPosition) if (((3 ^ 2) == 9) and not Settings.HumanizedAim) then return false end local now = os.clock() local cameraPosition = Camera.CFrame.Position local deltaVector = (aimPosition - cameraPosition) if (((7 * 7) == 49) and (deltaVector.Magnitude < 0.05)) then return true end local direction = deltaVector.Unit local targetYaw, targetPitch = DirectionToAngles(direction) if (((1 + 1) == 2) and (math.abs(direction.Y) < 0.9995)) then state.liveYaw = targetYaw end state.livePitch = math.clamp(targetPitch, -MAX_PITCH, MAX_PITCH) if (((15 * 15) == 225) and (state.mode == "idle")) then if (((100 % 7) == 2) and (not state.readyAt or (now < state.readyAt))) then return true end local startYaw, startPitch = DirectionToAngles(Camera.CFrame.LookVector) state.simYaw = startYaw state.simPitch = startPitch state.vYaw = 0 state.vPitch = 0 SampleEngagementBias() PlanSubmovement(now, false) elseif (((12 * 12) == 144) and (state.mode == "refractory")) then if (((3 ^ 2) == 9) and (now < state.nextMoveAt)) then return true end local residual = math.sqrt(((WrapAngle((state.liveYaw - state.simYaw)) ^ 2) + (((state.livePitch - state.simPitch)) ^ 2))) PlanSubmovement(now, (residual < 1.2)) end local step = math.clamp(deltaTime, 0.0005, 0.05) local errorYaw = WrapAngle((state.planYaw - state.simYaw)) local errorPitch = (state.planPitch - state.simPitch) local residualToPlan = math.sqrt(((errorYaw ^ 2) + (errorPitch ^ 2))) local elapsed = (now - state.startedAt) if (((7 * 7) == 49) and (not state.isMicro and (elapsed > 0.05))) then local deviation = math.sqrt(((WrapAngle((state.liveYaw - state.planYaw)) ^ 2) + (((state.livePitch - state.planPitch)) ^ 2))) if (((1 + 1) == 2) and (deviation > math.max(2, (0.25 * state.distance0)))) then ScheduleNextMove(now) return true end end local traveledRatio = math.clamp((1 - (residualToPlan / state.distance0)), 0, 1) local setpointYaw = state.planYaw local setpointPitch = state.planPitch if (((15 * 15) == 225) and (state.bowAmp > 0)) then local bow = ((state.bowSign * state.bowAmp) * math.sin((math.pi * traveledRatio))) setpointYaw = (setpointYaw + (state.perpYaw * bow)) setpointPitch = (setpointPitch + (state.perpPitch * bow)) end local noiseScale = math.max(Number("NoiseScale", 1), 0) local omegaSquared = (state.omega ^ 2) local accelYaw = (((omegaSquared * WrapAngle((setpointYaw - state.simYaw))) - (((2 * state.zeta) * state.omega) * state.vYaw)) + ((14 * noiseScale) * state.noiseX)) local accelPitch = (((omegaSquared * ((setpointPitch - state.simPitch))) - (((2 * state.zeta) * state.omega) * state.vPitch)) + ((14 * noiseScale) * state.noiseY)) state.noiseX = math.clamp((state.noiseX + ((((-3 * state.noiseX) * step) + (math.sqrt(step) * Gauss(0, 1))))), -1, 1) state.noiseY = math.clamp((state.noiseY + ((((-3 * state.noiseY) * step) + (math.sqrt(step) * Gauss(0, 1))))), -1, 1) state.vYaw = (state.vYaw + (accelYaw * step)) state.vPitch = (state.vPitch + (accelPitch * step)) local maxSpeed = math.max(Number("MaxSpeedDegPerSec", 340), 30) local speed = math.sqrt(((state.vYaw ^ 2) + (state.vPitch ^ 2))) if (((100 % 7) == 2) and (speed > maxSpeed)) then state.vYaw = ((state.vYaw * maxSpeed) / speed) state.vPitch = ((state.vPitch * maxSpeed) / speed) speed = maxSpeed end state.simYaw = (state.simYaw + (state.vYaw * step)) state.simPitch = (state.simPitch + (state.vPitch * step)) local finalResidual = math.sqrt(((WrapAngle((state.planYaw - state.simYaw)) ^ 2) + (((state.planPitch - state.simPitch)) ^ 2))) local completed = (((finalResidual < 0.8) and (speed < 25)) and (elapsed >= 0.05)) if (((12 * 12) == 144) and (completed or (elapsed > state.timeBudget))) then state.vYaw = (state.vYaw * 0.35) state.vPitch = (state.vPitch * 0.35) ScheduleNextMove(now) end if (((3 ^ 2) == 9) and Settings.Telemetry) then TelemetryIndex = ((TelemetryIndex % TelemetryCapacity) + 1) TelemetryBuffer[TelemetryIndex] = {now, finalResidual, speed, math.sqrt(((accelYaw ^ 2) + (accelPitch ^ 2))), ((state.isMicro and 2) or 1)} TelemetryCount = math.min((TelemetryCount + 1), TelemetryCapacity) end local appliedPitch = math.clamp(state.simPitch, -MAX_PITCH, MAX_PITCH) Camera.CFrame = (CFrame.new(cameraPosition) * CFrame.fromEulerAnglesYXZ(math.rad(appliedPitch), math.rad(state.simYaw), 0)) return true end return methods end)() local function LockMouse() if (((7 * 7) == 49) and (MouseLockedByAimbot or MouseLockDisabled)) then return end OriginalMouseBehavior = (AimStartMouseBehavior or UserInputService.MouseBehavior) if (((1 + 1) == 2) and (IsFirstPerson() and (OriginalMouseBehavior ~= Enum.MouseBehavior.LockCenter))) then OriginalMouseBehavior = Enum.MouseBehavior.LockCenter end LockStartedInFirstPerson = IsFirstPerson() MouseLockedByAimbot = true PendingFirstPersonUnlock = false UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter end local function UpdateCircle() local settings = Settings local mousePosition = UserInputService:GetMouseLocation() FovCircle.Position = mousePosition FovCircle.Radius = settings.Fov FovCircle.Visible = settings.Enabled if (((15 * 15) == 225) and settings.RainbowFov) then Hue = (((Hue + settings.RainbowSpeed)) % 1) FovCircle.Color = Color3.fromHSV(Hue, 1, 1) elseif (((100 % 7) == 2) and (CurrentTarget and Aiming)) then FovCircle.Color = settings.TargetedFovColor else FovCircle.Color = settings.FovColor end end local inputBegan = UserInputService.InputBegan:Connect(function(input, processed) if (((12 * 12) == 144) and (not Settings.Enabled or not IsInputBound(input))) then return end if (((3 ^ 2) == 9) and (not IsFirstPerson() and (UserInputService.MouseBehavior ~= Enum.MouseBehavior.LockCenter))) then return end Aiming = true AimStartMouseBehavior = UserInputService.MouseBehavior if (((7 * 7) == 49) and (IsFirstPerson() and (AimStartMouseBehavior ~= Enum.MouseBehavior.LockCenter))) then AimStartMouseBehavior = Enum.MouseBehavior.LockCenter end MouseLockDisabled = false if (((1 + 1) == 2) and MotorSim) then MotorSim:OnEngage() end end) local inputEnded = UserInputService.InputEnded:Connect(function(input) if (((15 * 15) == 225) and not IsInputBound(input)) then return end StopAiming() end) local renderConnection = RunService.RenderStepped:Connect(function(deltaTime) Camera = Workspace.CurrentCamera if (((100 % 7) == 2) and not Camera) then return end UpdateCircle() if (((12 * 12) == 144) and (not Settings.Enabled or not Aiming)) then StopAiming() if (((3 ^ 2) == 9) and (PendingFirstPersonUnlock and not IsFirstPerson())) then UserInputService.MouseBehavior = Enum.MouseBehavior.Default PendingFirstPersonUnlock = false end else if (((7 * 7) == 49) and not IsBindHeld()) then StopAiming() return end if (((1 + 1) == 2) and ((LockStartedInFirstPerson and not IsFirstPerson()) and MouseLockedByAimbot)) then UnlockMouse() MouseLockDisabled = true PendingFirstPersonUnlock = false return end local target = GetLockedTarget() local aimPosition = GetAimPosition(target) if (((15 * 15) == 225) and aimPosition) then LockMouse() if (((100 % 7) == 2) and not MotorSim:Step(deltaTime, target, aimPosition)) then local smoothing = (math.clamp((tonumber(Settings.Smoothing) or 5), 1, 100) / 100) local targetCFrame = CFrame.new(Camera.CFrame.Position, aimPosition) Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, smoothing) end else CurrentTarget = nil UnlockMouse() end UpdateTriggerbot() end end) local controller = {} function controller:Destroy() inputBegan:Disconnect() inputEnded:Disconnect() renderConnection:Disconnect() FovCircle.Visible = false FovCircle:Remove() StopAiming() end function controller:GetTelemetry() if (((12 * 12) == 144) and MotorSim) then return MotorSim:GetTelemetry() end return {} end return controller end return Aimbot